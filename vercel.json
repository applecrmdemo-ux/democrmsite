{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": null,
  "routes": [
    { "src": "^/api/(.*)", "dest": "/api/$1" },
    { "src": "/((?!api/)[^.]+)", "dest": "/", "status": 200 }
  ]
}
